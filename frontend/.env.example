# Local / Vercel environment (server-only — never commit real values)

OPENAI_API_KEY=
OPENAI_PROOFREAD_MODEL=gpt-5.6-luna

# Notes:
# - Do NOT use VITE_* for these secrets.
# - Browser never calls OpenAI directly.
# - POST /api/proofread is the only proofreading entry.
